# Machine Learning  Variatonal Inference
Variational Inference

http://latex.codecogs.com/gif.latex?\int _{ 0 }^{ \infty  }{ \lambda { e }^{ -\lambda x }\log { \lambda  }  } -\lambda x\lambda { e }^{ -\lambda x }
